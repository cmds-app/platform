# Contributions

The site is built from markdown in the [`cmds-app/docs`](https://github.com/cmds-app/docs) repository.

To propose changes, open a pull request against `main`.

Commits to `main` publish automatically via GitHub Actions.

Use the **pencil** icon in the top-right of any page to jump straight to its source on GitHub.
