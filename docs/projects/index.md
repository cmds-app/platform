# Projects

Per-project documentation for CMDS repositories.

## Current projects

- [**Bump**](bump.md) — semantic version management HTTP API for named packages.

As new projects mature to the point of public documentation, add a page under `docs/projects/<name>.md` and register it in the site navigation (`mkdocs.yml`).
